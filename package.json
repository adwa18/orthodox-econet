{
  "name": "orthodox-econet",
  "version": "1.0.0",
  "description": "የኦርቶዶክስ ኢኮኖሚ ኔትወርክ — Telegram Mini App for the Ethiopian Orthodox Christian business community",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "npm install --prefix backend && GENERATE_SOURCEMAP=false npm install --prefix frontend && npm run build --prefix frontend",
    "start": "node backend/src/server.js",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm start --prefix frontend"
  }
}
